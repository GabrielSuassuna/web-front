export const PostReport = {
    "required": ["feedbackId", "authorId"],
    "properties": {
        "feedbackId": {
            "type": "string",
        },
        "authorId": {
            "type": "string",
        }
    }
}