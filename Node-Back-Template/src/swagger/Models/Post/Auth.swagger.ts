export const PostAuth = {
    "required": ["code", "password"],
    "properties": {
        "code": {
            "type": "string"
        },
        "password": {
            "type": "string",
        }
    }
}