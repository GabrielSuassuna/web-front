export const PostTag = {
    "required": ["name", "description"],
    "properties": {
        "name": {
            "type": "string",
        },
        "description": {
            "type": "string",
        }
    }
}