export const PostStudent = {
    "required": ["registration", "name", "password"],
    "properties": {
        "registration": {
            "type": "string",
        },
        "name": {
            "type": "string"
        },
        "password": {
            "type": "string"
        },
    }
}