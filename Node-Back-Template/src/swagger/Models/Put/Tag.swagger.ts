export const PutTag = {
    "required": ["name", "description"],
    "properties": {
        "name": {
            "type": "string",
        },
        "description": {
            "type": "string",
        }
    }
}