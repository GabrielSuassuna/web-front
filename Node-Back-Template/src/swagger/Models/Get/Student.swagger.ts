export const GetStudent = {
    "required": ["id", "registration", "name"],
    "properties": {
        "id": {
            "type": "string"
        },
        "registration": {
            "type": "string",
        },
        "name": {
            "type": "string"
        },
    }
}