export const GetNotification = {
    "required": ["id", "message"],
    "properties": {
        "id": {
            "type": "string",
        },
        "message": {
            "type": "string",
        }
    }
}