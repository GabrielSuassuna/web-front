export const Department = {
    "required": ["id", "name"],
    "properties": {
        "id": {
            "type": "string",
        },
        "name": {
            "type": "string"
        },
    }
}